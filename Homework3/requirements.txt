# Core
# numpy<2: torch 2.2.x (останній wheel для Intel Mac) скомпільований проти numpy 1.x
numpy>=1.26,<2.0
psutil>=5.9
tqdm>=4.66

# Embeddings
# torch 2.2.2 — останній torch з macOS x86_64 wheel (Intel Mac).
# transformers<4.50 — щоб не вимагав torch>=2.4.
# sentence-transformers<3.1 — узгоджено з transformers 4.4x.
torch>=2.1,<2.3
transformers>=4.41,<4.50
sentence-transformers>=2.7,<3.1

# Datasets
datasets>=2.18

# Vector DBs
faiss-cpu>=1.8
qdrant-client>=1.9
chromadb>=0.5
psycopg[binary]>=3.1
pgvector>=0.3

# Plotting
matplotlib>=3.8
pandas>=2.2

# Optional: OpenAI embeddings
# openai>=1.30
